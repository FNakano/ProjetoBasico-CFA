import startsystem # execute my (a micropython user) code this is the right file to put it on
# firmware somehow detects if Thonny is trying to connect through USB and avoid running main.py.
# with this device connected to USB, press its RESET button and 'immediately' press Thonny's STOP button
# This should result in Thonny presenting REPL prompt (>>>) instead of not responding.
# you might have to try two or three times before it takes effect
